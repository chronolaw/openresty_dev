<?php
    // Copyright (C) 2018 by chrono
    //
    // sudo apt-get update
    // sudo apt-get install apache2 --fix-missing
    // sudo apt-get install php (or php5)
    // sudo apt-get install libapache2-mod-php (in ubuntu 1604)
    //
    // sudo /etc/init.d/apache2 start
    // sudo /etc/init.d/apache2 stop
    //
    // sudo cp hello.php /var/www/html/
    // curl http://127.0.0.1/hello.php
    // to use http_load, you must modify http_load_url
    //
    // ab -c 100 -n 10000 http://127.0.0.1/hello.php

    echo "hello, world\n";
?>
